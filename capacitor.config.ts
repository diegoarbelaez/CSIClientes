import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  // appId: 'io.ionic.starter',
  appId: 'io.csi.seguridad.colombia',
  appName: 'CSI Seguridad',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
